apiVersion: apps/v1
kind: Deployment
metadata:
  name: iris-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iris-dev
  template:
    metadata:
      labels:
        app: iris-dev
    spec:
      containers:
        - name: iris-container
          image: 494722910828.dkr.ecr.us-east-2.amazonaws.com/iris-api:latest
          ports:
            - containerPort: 5000

